language: go
go: 1.13.x

script: echo "success!"

deploy:
- provider: script
  skip_cleanup: true
  script: cd cacli && curl -sL https://git.io/goreleaser | bash
  on:
    tags: true
    condition: $TRAVIS_OS_NAME = linux